// @flow strict

import * as React from 'react';

import { SearchFormWrapper, SearchInput } from './SearchForm.styles';

const SearchForm = () => (
  <SearchFormWrapper>
    <SearchInput placeholder="search" />
  </SearchFormWrapper>
);

export default SearchForm;
