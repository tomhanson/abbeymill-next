import styled from 'styled-components';

const DownArrow = styled.div`
  z-index: 99;
  position: absolute;
  bottom: 20px;
  left: 50%;
  margin-left: -20px;
  margin-right: auto;
  cursor: pointer;
`;

export default DownArrow;

// z-index: 99;
